import { ref } from 'vue'

export function useCRUD(config) {
  const loading = ref(true)

  // 前置处理
  const beforeFunc = () => {

  }

  // 后置处理
  const afterFunc = () => {

  }

  const createApi = async (api, data) => {

  }

  const readApi = async (api, params) => {

  }

  const updateApi = async (api, data) => {

  }

  const deleteApi = async (api, data) => {

  }
}
